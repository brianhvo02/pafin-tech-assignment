import { Router } from 'express';

const UserRouter = Router();

UserRouter.post('/:id', () => {
    
});

UserRouter.get('/:id', () => {

});

UserRouter.patch('/:id', () => {
    
});

UserRouter.delete('/:id', () => {
    
});

export default UserRouter;