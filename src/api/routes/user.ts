import { Router } from 'express';

const UserRouter = Router();

UserRouter.get('/:id', () => {

});

UserRouter.patch('/:id', () => {
    
});

UserRouter.delete('/:id', () => {
    
});

export default UserRouter;